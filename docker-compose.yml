services:
  cookie-server:
    build: ./backend
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - openclaw-net

networks:
  openclaw-net:
    name: openclaw-docker-cn_default
    external: true
